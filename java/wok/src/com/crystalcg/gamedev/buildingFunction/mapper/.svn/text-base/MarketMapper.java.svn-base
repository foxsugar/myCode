package com.crystalcg.gamedev.buildingFunction.mapper;

import com.crystalcg.gamedev.buildingFunction.domain.UserMarket;

public interface MarketMapper {
	/**
	 * 获取集市兑换记录信息
	 * @param characterId
	 * @return
	 */
	UserMarket getUserMarket(int characterId);
	/**
	 * 更新集市兑换记录信息
	 * @param userMarket
	 */
	void updateUserMarket(UserMarket userMarket);
	/**
	 * 插入集市兑换记录信息
	 * @param characterId
	 */
	void insertUserMarket(int characterId);
	
	/**
	 * 删除资源兑换信息
	 * @param characterId
	 */
	void deleteUserMarket(int characterId);
}
